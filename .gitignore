# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# recording / media content (repo stays without interview data or built assets)
# Ignore all of public except carousel images (for templates/PRs)
/public/*
!/public/images/
/public/images/*
!/public/images/carousel*
weaviate-data.tar.gz

# json: ignore project interview data and generated counts; keep templates and schemas for PRs/forks
/json/entity-recording-counts.json
!/json/interviews/example-collection/

# local/deploy scripts and docs (not for upstream PR)
setup-nginx-ssl.sh
backup-for-repo-recreate.sh
RESTORE_AFTER_REFORK.md

# misc
.DS_Store
.vscode

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env.local
.env.cloud
.env.production

# organization-specific configuration
# Each organization should maintain their own config.json
# Use config.example.json as a template
config.json

# TypeScript build cache
tsconfig.tsbuildinfo

# Yarn Berry
.yarn/cache
.yarn/unplugged
.yarn/install-state.gz
.yarn/build-state.yml
.pnp.*.env.cloud
